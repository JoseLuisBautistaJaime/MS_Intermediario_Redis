---
applications:
- name: ms_intermediario_redis
 #  instances: 1
 # host: rest-service
 # domain: guides.spring.io
  path: build/libs/ms_intermediario_redis-0.0.1-SNAPSHOT.jar

  env:
    JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+}}'
